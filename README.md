# Yandex Young Internship — Вступительные задания

Репозиторий содержит решения задач со вступительных испытаний **Yandex Young Internship**.  
Задачи решены на языке **C++** и оформлены в виде отдельных файлов.

## 🧩 Описание алгоритмов по задачам

### 🅰️ A.cpp — Поиск максимального пути в графе

В данной задаче решается проблема поиска **максимального по весу простого пути** в неориентированном графе, начиная с фиксированной вершины.

#### Используемые алгоритмы и идеи:
- **Динамическое программирование по битмаске (Bitmask DP)**  
  Состояние `dp[mask][v]` хранит максимальную сумму значений вершин для пути, который:
  - проходит через набор вершин `mask`
  - заканчивается в вершине `v`

- **Перебор подмножеств вершин**  
  Используется битовая маска для отслеживания уже посещённых вершин, что гарантирует отсутствие циклов.

- **Граф в виде списка смежности**  
  Эффективное хранение и обход рёбер графа.

#### Сложность:
- Время: `O(2^n * n * deg)`
- Память: `O(2^n * n)`

Подход применим для малых `n` и типичен для задач на полный перебор состояний.

---

### 🅲 C.cpp — Объединение запросов по общим словам

Задача сводится к объединению запросов в группы (контексты), если они содержат **хотя бы одно общее слово**, и подсчёту характеристик этих групп.

#### Используемые алгоритмы и структуры данных:
- **Система непересекающихся множеств (DSU / Union-Find)**  
  Используется для объединения индексов запросов, имеющих общее слово.

- **Сжатие путей и объединение по рангу**  
  Обеспечивает почти константную амортизированную сложность операций.

- **Хеш-таблицы (`unordered_map`, `unordered_set`)**  
  - сопоставление слов с первым встретившимся запросом
  - хранение уникальных слов внутри каждого контекста

#### Итог:
- определяется количество различных контекстов
- находится максимальное число уникальных слов в одном контексте

#### Сложность:
- Время: `O(N α(N) + W)`
- Память: `O(W)`, где `W` — общее количество слов

---

### 🅳 D.cpp — Монотонный стек с разбиением по чётности

В задаче для каждого элемента массива высот требуется найти расстояние до ближайшего справа элемента:
- с **большей высотой**
- и **той же чётностью индекса**

#### Используемые алгоритмы:
- **Монотонный стек**  
  Поддерживается убывающий стек индексов по значениям высот.

- **Два независимых стека**  
  Отдельные стеки для чётных и нечётных индексов позволяют учитывать условие чётности.

- **Обход массива справа налево**  
  Гарантирует, что в стеке находятся только кандидаты справа от текущего элемента.

#### Сложность:
- Время: `O(N)`
- Память: `O(N)`

Алгоритм эффективно решает задачу поиска следующего большего элемента с дополнительными ограничениями.

---

## 📌 Общие используемые подходы

- Динамическое программирование
- Теория графов
- DSU (Union-Find)
- Монотонные стеки
- Хеш-структуры данных
- Оптимизация по времени и памяти

## 📁 Структура проекта

```text
.
├── A.cpp
└── B.cpp
├── C.cpp
└── D.cpp
└── E.cpp
